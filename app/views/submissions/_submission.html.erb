<tr>
  <td><%= submission.name %></td>
  <td><%= submission.description %></td>
  <td>
    <%= link_to 'view', submission_path(submission) %>
    | <%= link_to 'edit', edit_submission_path(submission) %>
    | <%= link_to "delete", submission, method: :delete, data: { confirm: "You sure?" } %>
  </td>
</tr>